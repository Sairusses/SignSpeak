import 'package:flutter/material.dart';

class TextToSignPage extends StatelessWidget{
  const TextToSignPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text("Text to Sign Page"),
        )
    );
  }
}